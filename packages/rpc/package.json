{
  "name": "@finney/rpc",
  "version": "0.0.3",
  "description": "A dependency-free TypeScript client for Bitcoin Core's JSON-RPC",
  "main": "./dist/index.js",
  "files": [
    "dist/package.json",
    "dist/*.js",
    "dist/**/*.js",
    "dist/**/*.d.ts",
    "dist/*.d.ts"
  ],
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf /dist; tsc ./index.ts --outDir ./dist --declaration",
    "test": "bun wiptest"
  },
  "devDependencies": {
    "bun-types": "^0.5.0"
  }
}